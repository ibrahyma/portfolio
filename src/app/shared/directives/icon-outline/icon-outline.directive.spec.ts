import { IconOutlineDirective } from './icon-outline.directive';

describe('IconOutlineDirective', () => {
  it('should create an instance', () => {
    const directive = new IconOutlineDirective();
    expect(directive).toBeTruthy();
  });
});
